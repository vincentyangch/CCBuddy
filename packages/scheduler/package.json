{
  "name": "@ccbuddy/scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ccbuddy/core": "*",
    "cron-parser": "^5.5.0",
    "node-cron": "^3"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/node-cron": "^3",
    "vitest": "^3"
  }
}
